spring.application.name=mini-saas
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=jdbc:postgresql://ep-frosty-moon-ap0oblth-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_OB43EIAqXDhR
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Execute schema.sql (for triggers/procedures) on startup
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
spring.sql.init.separator=//
spring.jpa.defer-datasource-initialization=true

# Security configuration will be handled in SecurityConfig.java
